{{- if .Values.namespace.create }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ include "update-controller.namespace" . }}
  labels:
    {{- include "update-controller.labels" . | nindent 4 }}
{{- end }}
